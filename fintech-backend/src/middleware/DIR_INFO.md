**Description**
Functions that run during the request-response cycle (e.g., authMiddleWare). Use these for authentication checks, input validation, and logging before a request reaches the controller.
